# Hook/场景通过替换模板提示词句段生效，而非独立结构化输入

模板态下的 Hook 与场景选择器直接替换提示词文本中对应的句段（模板提示词按 Hook 句 / 场景句 / 商品句 / CTA 句分段存储），保持"提示词即所见"——用户最终提交的就是输入框里的完整文本。若用户手改导致句段在文本中不再可匹配，则降级为在末尾追加。被否的替代方案：Hook/场景作为独立 chips 随提示词一起提交（用户看不到它们如何影响最终提示词，与"模板填充提示词"的心智模型断裂）。此决策要求模板数据结构必须携带句段边界信息，且 Hook/场景选择器仅在模板态出现——自由输入的文本没有句段结构可供替换。
